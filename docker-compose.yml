services:
  app:
    build: .
    container_name: amigas-estoque-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/usr/src/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/usr/src/app/data/estoque.db
